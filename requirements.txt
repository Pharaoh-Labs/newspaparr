# Web UI framework
flask>=3.0.0
flask-wtf>=1.2.0
flask-sqlalchemy>=3.1.0
flask-migrate>=4.0.0
wtforms>=3.1.0

# Scheduling
apscheduler>=3.10.0

# Configuration
pytz>=2023.3
python-dateutil>=2.8.0

# Database
sqlalchemy>=2.0.0

# Renewal HTTP client (replaces the old selenium browser pipeline)
httpx>=0.27.0

# Cookie decryption (Chrome v10 AES-CBC + PBKDF2)
cryptography>=42.0.0

# WSGI server
gunicorn>=21.0.0

# Browser-in-browser capture (noVNC bridge to a real Chrome)
websockify>=0.11.0

# Notifications via ~80 services (discord, ntfy, mailto, slack, gotify, ...).
# Configure with APPRISE_URLS env var (comma-separated apprise:// URLs).
apprise>=1.9.0
